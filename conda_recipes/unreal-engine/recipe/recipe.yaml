context:
  name: "unrealengine"
  version: "5.6"
  # build_number: Used for both version ordering (number) and filename (string).
  # Increment when rebuilding the same version. Output: unrealengine-{version}-{build_number}.conda
  build_number: 9

  
package:
  name: ${{ name }}
  version: ${{ version }}

source:
  - path: 'C:\Program Files\Epic Games\UE_${{ version }}\Engine'

build:
  number: ${{ build_number }}
  string: ${{ build_number }}
  script:
    - bash %RECIPE_DIR%\build.sh
  files:
    exclude:
      # Directories to exclude
      - "**/Documentation/**"
      - "**/Extras/**"
      - "**/Samples/**"
      - "**/Templates/**"
      - "**/FeaturePacks/**"
      - "**/Intermediate/**"
      - "**/DerivedDataCache/**"
      - "**/Saved/**"
      - "**/Build/**"
      - "**/Source/**"
      - "**/Restricted/**"
      # Build artifacts
      - "**/*.ipdb"
      - "**/*.iobj"
      - "**/*.exp"
      - "**/*.ilk"
      - "**/*.obj"
      - "**/*.log"
      # Source code files
      - "**/*.cpp"
      - "**/*.c"
      - "**/*.inl"
      - "**/*.hpp"
      - "**/*.cs"
      # Other development files
      - "**/*.vcxproj"
      - "**/*.sln"
      - "**/*.filters"
      - "**/*.user"
      - "**/*.rc"
      - "**/*.manifest"
      - "**/*.pdb"
      - "**/*.lib"

tests:
  - script:
      - if not exist "%PREFIX%\unreal-engine\UE_${{ version }}\Engine\Binaries\Win64\UnrealEditor.exe" exit 1

about:
  homepage: https://www.unrealengine.com/